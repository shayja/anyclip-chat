{
  "name": "anyclip-chat-server",
  "version": "1.0.0",
  "description": "AnyClip chat server from Shay Jacoby",
  "main": "server.js",
  "scripts": {
    "lint": "eslint --config .eslintrc.json --ignore-path .gitignore .",
    "start": "node server.js",
    "start:dev": "npm run lint && nodemon ./server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shay Jacoby",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/storage": "^5.2.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.1.0",
    "sequelize": "^6.3.5",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "nodemon": "^2.0.4"
  }
}
